/*
In the array declaration
double score[5];
identify the following:
a. The array name
b. The base type
c. The declared size of the array
d. The range of values an index accessing this array can have
e. One of the indexed variables (or elements) of this array
*/

// Answer:
// a. The array name is "score".
// b. The base type is "double".
// c. The declared size of the array is 5.
// d. The range of values an index accessing this array can have is 0 to 4.
// e. One of the indexed variables (or elements) of this array is "score[4]", the last element of the array.
