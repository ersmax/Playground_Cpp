/*
Give the declaration for two variables called feet and inches. Both variables are
of type int and both are to be initialized to zero in the declaration. Give both
initialization alternatives.
*/

int main() {
    int feet = 0, inches = 0;
    int feet2(0), inches2(0);
    return 0;
}